android.useAndroidX=true
kotlin.code.style=official

# Give the Gradle daemon (and the in-process D8 dexer) enough heap. The default
# 512m ceiling intermittently OOMs during mergeDexDebug on CI runners.
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
